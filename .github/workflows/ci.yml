name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    name: Build, TypeScript, tests and security
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: TypeScript check
        run: bun run typecheck

      - name: Lint report
        run: bun run lint:ci

      - name: Unit tests
        run: bun run test

      - name: Production build
        run: bun run build

      - name: App security rules
        run: bun run security:scan

      - name: Sitemap validation (local)
        run: bun run sitemap:validate

      - name: Sitemap validation (HTTP 200 against production)
        run: bun run sitemap:validate:http
        env:
          BASE_URL: https://clemio.app

      - name: RLS policy matrix
        env:
          RLS_DATABASE_URL: ${{ secrets.RLS_DATABASE_URL }}
        run: bun run security:rls
